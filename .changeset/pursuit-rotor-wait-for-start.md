---
"@jspsych-contrib/plugin-pursuit-rotor": major
---

1.0 release. Add `wait_for_start` parameter (default `true`) so the rotor stays stationary until the participant clicks or taps. Add `start_text` parameter for customizable prompt text shown while waiting (set to `null` or `""` to hide).
