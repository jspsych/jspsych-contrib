---
"@jspsych-contrib/plugin-corsi-blocks": minor
---

Add new timing and visual parameters for enhanced flexibility

- Add `post_sequence_delay` parameter (default 500ms) - delay after sequence before response enabled
- Add `inter_trial_delay` parameter (default 1500ms) - pause between trials
- Add `response_timeout` parameter (default null) - maximum time for response
- Add `response_animation_duration` parameter (default 200ms) - click feedback duration
- Add `background_color` parameter (default '#000000') - display background color
- Update default `response_animation_duration` from 500ms to 200ms
- Add comprehensive examples demonstrating new parameters
